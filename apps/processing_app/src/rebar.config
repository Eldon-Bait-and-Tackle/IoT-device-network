{apps, [processing_app]}.

{erl_opts, [debug_info]}.

{deps, [
    %% Add dependencies here, e.g.:
    %% {cowboy, "2.9.0"} 
]}.

{shell, [
  %% {config, "config/sys.config"},
  {apps, [processing_app]}
]}.
